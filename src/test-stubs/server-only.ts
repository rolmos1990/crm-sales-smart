// Stub para Vitest: `server-only` lo resuelve Next en el build (lanza si se
// importa desde un Client Component). En tests unitarios no hay bundler, así
// que el paquete no existe y cualquier módulo que lo importe no cargaría.
export {};
